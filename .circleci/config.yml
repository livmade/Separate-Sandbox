version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: livmade/boop
    steps:
      - run: echo "Hi I'm on Runners!"
      - run: echo "What's good?"
      - run: echo "I liek chocolate"
